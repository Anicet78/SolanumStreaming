meta {
  name: add_to_collection_other
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/collection
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "movie_id": 553301
  }
}

vars:pre-request {
  baseUrl: http://localhost:8082
}

tests {
  test("Returns approriate status code", function() {
    expect(res.status).to.equal(204);
  });
}

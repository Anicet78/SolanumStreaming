meta {
  name: get_in_collection_invalid
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/collection/194
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:8082
}

tests {
  test("Returns approriate status code", function() {
    expect(res.status).to.equal(404);
  });
}
